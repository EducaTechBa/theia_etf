{
  "private": true,
  "scripts": {
    "prepare": "lerna run prepare",
    "prepare-dev": "lerna run prepare-dev",
    "rebuild:browser": "theia rebuild:browser"
  },
  "devDependencies": {
    "lerna": "2.4.0"
  },
  "workspaces": [
    "uup-game-view", "top-bar", "autotest-view", "assignments-view", "app"
  ]
}
